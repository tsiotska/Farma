import { createStyles } from '@material-ui/core';

export const styles = (theme: any) => createStyles({
    root: {
        margin: '10px 0 6px',
    },
    input: {},
    labelRoot: {},
    helperText: {}
});
